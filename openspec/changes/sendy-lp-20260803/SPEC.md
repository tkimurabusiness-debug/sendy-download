# SPEC: sendy-lp-20260803 — 紹介ページを「短く・具体的に・AI と人で分ける」

対象リポジトリ `/Users/kimuratakezou/uicos/sendy-download-page`
参照した実装 `/Users/kimuratakezou/uicos/app-messenger` (**読み取り専用。1バイトも書き換えない**)

---

## 1. 要件一覧 (竹蔵の言葉をそのまま引用 + 受入条件)

| # | 竹蔵の言葉 (引用) | 受入条件 (機械または目視で確かめられる形) | 状態 |
|---|---|---|---|
| R1 | 「LPがどんどん汚くなってるから修正したいかも」 | 実在の製品ページを採寸して並べ方を移植し、出所を `REFERENCES.md` に1行ずつ書く | 済 |
| R2 | 「現状文字多すぎだし長すぎ」 | ページ全体の高さが 1440x900 で 20.2画面から**減る**こと。1機能あたりの説明は**2〜3行以内**。それを超える話は `<details>` に畳む | 済 |
| R3 | 「AIに送るところのやつが全然機能が具体的じゃなくてこれじゃあ説明できないでしょ。もっと具体的にする」 | AI 向けの節に、機能名 / 何ができるか / どの接続先で使えるか / 対応OS / 値段 が**項目の並び**として入る。項目数 40 以上 | 済 (44項目) |
| R4 | 「もっと相手に刺さるように言ったほうがいいかな」 | 見出しは**機能の名前ではなく困りごとが消えること**を書く (例「AI 返信サジェスト」→「返しそびれが無くなる」) | 済 |
| R5 | 「ちゃんと人によって刺さるように明確なキャッチ＋詳細な説明みたいな感じで記載しておいて欲しい」 | 使う人の型ごとの入口を3つ作る。型の分け方の根拠を報告に書く。1機能 = キャッチ1行 + 説明2〜3行 + 折り畳みの詳細 | 済 |
| R6 | 「AIが見る用のところと人間が読む用のところは別にして欲しい」 | 人が読む節 (`#can`) と AI 向けの節 (`#for-ai`) が**別の `<section>`**であること。`llms.txt` が `#for-ai` と一致すること | 済 |
| R7 | 竹蔵が挙げた機能15個を全部載せる + 「多分もっとあるからそれらももっと具体化して載せてあげて」 | 15個を実装で仕分けし、「今できる」だけを断定で書く。ほかに実装で確認できた機能も足す | 済 (今できる23個 / これから9個) |
| R8 | (既存の掟) 見出し・ボタンの文言に「、」「。」を入れない | `node scripts/check-page.mjs` が h1〜h6 / summary / button / .aud-link / .cap-kicker / .prov-name / .feat-label を全部見て 0 件 | 済 |
| R9 | (既存の掟) 絵文字を1つも使わない | 同スクリプトが `index.html` と `llms.txt` で 0 件 | 済 |
| R10 | (既存の掟) 0から CSS を書かない | 新しい色・角丸・グラデを1つも増やさない。使うのは `css/00-base.css` の `:root` にある値だけ | 済 |
| R11 | (対向レビューで🔴として直された経緯) 出来ていない機能を一覧に載せない | 「今できる」に置く機能はすべて **UI の入口 + main 側の処理** の両方を file:line で確認済みであること | 済 |

## 2. 調査 (実機ではなくコードでの裏取り。証拠は file:line)

`/Users/kimuratakezou/uicos/app-messenger` を読み取り専用で調べ、竹蔵が挙げた15個を
**今できる / 一部だけ / まだ無い** の3つに仕分けた。結果の全文は最終報告に載せる。要点だけ:

- **今できる (ページに断定で書いてよい)**: 返信案 / カレンダーの日程提案 / 音声入力 / 文の整形 /
  AI チャット / 既読を送信時だけ付ける (既定 on-send) / 会話の書き出し / MCP / LINE の主要操作 /
  文体と相手の記憶 / 1ウィンドウ / 最近のファイルのトレイ / コマンドパレット
- **一部だけ (断定で書かない・範囲を限定して書く)**:
  - API = 読み取りと下書きの投入はできるが、**送信は必ず人が承認**する。「完全自動化」は不可
    (`docs/api.md:21`「送信はできません。/v1/replies は下書きを積むだけで」)
  - 複数アカウント = **LINE とメールだけ**。ほかの接続先は1つずつ
  - Superhuman モード = 「返す必要のあるものだけ出す」までは動く。**「返したら次が出る」は無い**
    (`src/renderer/ui/ThreadView.tsx` の送信後に次のスレッドを選ぶ処理が存在しない)
- **ページに載せない**: 自動タグ付け (`src/main/tagging/TagLLM.ts:6` に `TODO(wire-up)` が残る死んだコード)

## 3. 実装プラン (どのファイルに何を書くか)

| ファイル | 何をするか |
|---|---|
| `REFERENCES.md` | 新規。Linear / Notion Calendar / Superhuman / Raycast の採寸値と「何を借りたか」 |
| `index.html` | ナビの項目を差し替え / ヒーローの重複を削る / Messenger の手順を `<details>` へ / 対応サービスを3群に作り直す / 強みの節を5→3 / `#audience` `#can` `#next` `#for-ai` を新設 |
| `css/40-features.css` | `.aud-*` `.cap-*` `.feat--ai` `.prov-note` `.note--fold` を追記。**新しい色と角丸は作らない** |
| `css/10-features.css` | `.fx2` の高さ 850vh → 340vh |
| `css/20-services.css` | `.svc-stage` の高さ 420vh → 220vh |
| `css/00-base.css` | 狭い画面のヒーローの余白の二重取りを直す |
| `js/features.js` | 連番画像の長さを「文章の数」から受け取る (直書きの 5 をやめる) |
| `js/feedback.js` | 一覧の組み立てをやめ、押せるようにする処理だけ残す (素の HTML を機械にも読ませるため) |
| `llms.txt` | `#for-ai` と同じ中身にそろえる |
| `scripts/check-page.mjs` | 新規。句読点 / 絵文字 / llms.txt とのずれ / 隠し指定 を機械で止める |

## フォーク調査結果

竹蔵の方針 (memory `feedback_implementation_policy.md` / `~/.config/ai-rules/CSS.md`) は
「0から書かず、実在する物を下敷きにする」。この作業でも**先に外を調べてから**手を動かした。

### 調べたもの と 採用 / 非採用の理由

| 調べたもの | 何か | 採用したか | 理由 |
|---|---|---|---|
| Linear の機能一覧 `https://linear.app/features` | 実在の SaaS 紹介ページ | **採用** | 機能が多いのに1つあたり「分類名13px + 1文20px」だけで見せている。今回の「23個を短く見せる」に完全に合う。実測値をそのまま `.cap-*` に移植 |
| Notion Calendar `https://www.notion.com/product/calendar` | 実在の製品ページ | **採用** | 見出しが「See your schedule at a glance」「No more double bookings」のように**結果の言い切り**。竹蔵の「刺さるように」の見本。3列グリッドの数値も採用 |
| Superhuman `https://superhuman.com/` | 実在の製品ページ | **採用** | Solutions を役割 (Enterprises / Education / Marketing teams / IT teams) で分けている。「人によって刺さるように」の入口の作り方をここから借りた |
| Raycast `https://www.raycast.com/` | 実在の製品ページ | **採用** | 名前と値を並べる格子 (`147.9px + 311.1px` / gap `16px 24px`) と、説明ゼロの密な索引。AI 向けの節の密度に使った |
| Tailwind UI / Flowbite などの汎用 UI キット | 有料・無料のコンポーネント集 | **非採用** | 既存の `css/00-base.css` が持つトークン (Jet Black + 青紫の2色) と別の設計思想で、入れると土台が二重になる。竹蔵の掟「基準CSSの上に積む」に反する |
| Astro / Tailwind などの枠組みへの載せ替え | 静的サイトの枠組み | **非採用** | 竹蔵の指示「依存パッケージを足さない (素の HTML/CSS/JS のまま)」。今回の依頼の範囲外 |
| 既存の `js/features.js` `js/services.js` の演出 | 既にこのページに入っている GSAP 移植コード (GSAP 公式 imageSequence / Codrops folding-cardboard-box) | **そのまま流用** | 動いている物を作り直さない。長さ (vh) だけ短くした |

### 採寸の生データ

`~/.claude/bin/css-harvest` と Chrome DevTools の `getComputedStyle` で 2026-08-03 に実測。

| ページ | 実測 | 使い道 |
|---|---|---|
| linear.app/features | grid `476px 476px` / gap 24px / card padding `32px 24px` / 分類名 13px #8a8f98 / 主役の1文 20px | `.cap-*` の作り |
| notion.com/product/calendar | 3列 `248.6px` / column-gap 24px / 見出しが結果の言い切り | `.aud-grid` と文言の言い方 |
| superhuman.com | 役割で入口を分ける / 効きめで言い切る見出し | `#audience` の設計 |
| raycast.com | 名前と値の格子 `147.9px + 311.1px` / gap `16px 24px` | `.cap-keys` と `.feat--ai` の密度 |

生データ `/tmp/harvest-linear.json` `/tmp/harvest-raycast.json` `/tmp/harvest-superhuman.json`

## MECE宣言

### 枠の宣言 (採用した軸 / 捨てた軸)

- **採用した軸 = 「読み手 × 節 × 画面幅」**。
  - 読み手 = 人 / AI (竹蔵が「別にして欲しい」と言った分け方そのもの)
  - 節 = ヒーロー / 対応サービス / 強み / 入口 / できること / これから / AI 向け / 会社
  - 画面幅 = 1440 (PC) / 390 (スマホ)
- **捨てた軸**: 「機能の技術的な分類 (接続 / AI / データ)」。これは作り手の都合の分け方で、
  読む人の困りごとと対応しない。竹蔵の「人によって刺さるように」に反するので採らない。

### 11枠の網羅 (空セル0)

| # | 枠 | このページでの定義 |
|---|---|---|
| 1 | 異常系 | JavaScript が動かない読み手 → 機能の一覧と AI 向けの事実を**素の HTML** に置いたので全部読める (以前は 0 件見えていた)。GSAP が読めない → `body.fx2-static` / `svc-static` で縦積みの静止表示に落ちる (既存)。連番画像が 404 → `js/features.js` の `updateImage` が例外を外に出さず静止画に落ちる (既存)。フォームの送信が失敗 → 画面は壊さず1行で伝える (既存)。`prefers-reduced-motion` → 全部の動きを止める (既存) |
| 2 | 非機能 | ページ全体の高さを **20.2画面 → 減らす** (数値目標)。console のエラー **0件**。横にはみ出す要素 **0件** (390幅)。追加する依存パッケージ **0個** |
| 3 | ロールバック | 静的ファイルだけの変更なので `git revert` 1回で完全に戻る。データも設定も持たない。push はしないので、竹蔵が見て気に入らなければ捨てられる |
| 4 | データ移行・後方互換 | 保存する物が無いので移行なし。**外向きの住所 (URL) は1つも変えない** — `#features` `#download` `#providers` `#company` は残す。`#ask-ai` と `#roadmap` は `#for-ai` `#next` に変わるので、ヘッダーとフッターの案内も同時に直した (リンク切れ0) |
| 5 | UI 4状態 + 死にボタン0 | 空 = 「これから足すもの」は必ず9件あるので空にならない。読込 = 機能の一覧は素の HTML なので読込中の空白が無い。エラー = フォームの3つとも失敗時の文言あり。最長文言 = 一番長い機能名「LINE 以外でも複数アカウント」で 390幅・1440幅の両方を目視。**死にボタン0** = 押せる要素は「コピーする」「知らせてもらう」「送る」「これから足すものの札」「もう少し詳しく」の5種だけで、全部その場で動く |
| 6 | 並行性 | 静的ページなので同時編集なし。フォームの連打 → 受け口 (Cloudflare Worker) 側の既存の絞りに任せる (今回変えない) |
| 7 | 権限・ロール差分 | 読む人に権限の区別が無い。**対象外** |
| 8 | 観測性 | 今回、計測の仕掛けは足さない。**対象外 (保留: 竹蔵から計測の依頼が無いため。必要になったら別の作業で足す)** |
| 9 | TZ・i18n・文字コード | 日本語のみ。`<html lang="ja">` と UTF-8 は既存のまま。見出しの折り返しは `word-break: keep-all` + `<wbr>` で語が割れないようにする (既存の作法を踏襲) |
| 10 | スコープ外宣言 (やらないこと) | ① `app-messenger` を1バイトも書き換えない ② push しない ③ 値段・実績の数字を作らない ④ 依存パッケージを足さない ⑤ ヒーローの左右分割・アイコンが集まる演出・巨大ワードマークなど**承認済みの土台の見た目は変えない** (長さだけ短くする) |
| 11 | 運用・CS影響 | 「Messenger の履歴が出ない」の手順を畳んだので、**問い合わせが増える可能性がある**。消さずに同じ場所に残し、見出しは今までどおり「Messenger の過去のやり取りが出てこないとき」のままにして探せるようにした |

### 全射トレース (要件 ↔ 受入条件 ↔ 実装タスク)

forward = R1〜R11 のすべてに受入条件が1つ以上ある。
backward = 下の実装タスクはすべて親要件を正確に1つ持つ (親の無い変更は入れていない)。

| 要件ID | 受入条件 (測り方) | 実装タスク (触るファイル) |
|---|---|---|
| R1 | 借りた出所が1行ずつ書いてある | T1 `REFERENCES.md` を作る |
| R2 | ページ全体の高さが 20.2画面から減る | T2 `css/10-features.css` の `.fx2` を 850vh→340vh / T3 `css/20-services.css` の `.svc-stage` を 420vh→220vh / T4 `index.html` のヒーローの重複2行と箇条書き2件を削る / T5 Messenger の手順を `<details>` に畳む / T6 `js/features.js` の連番の長さを文章の数から受け取る / T7 `css/00-base.css` の狭い画面の余白の二重取りを直す |
| R3 | AI 向けの節の項目数が40以上 | T8 `index.html` に `#for-ai` を作る (44項目) |
| R4 | 見出しが機能名でなく困りごとが消えること | T9 `index.html` の `.cap-title` 23件を言い換える |
| R5 | 入口が3つ / 1機能 = 見出し1行 + 説明2〜3行 | T10 `index.html` に `#audience` を作る / T11 `css/40-features.css` に `.aud-*` `.cap-*` を足す |
| R6 | 人の節と AI の節が別 `<section>` / `llms.txt` と一致 | T8 と T12 `llms.txt` を書き直す / T13 `js/feedback.js` の一覧の組み立てをやめる / T14 `scripts/check-page.mjs` の一致検査 |
| R7 | 15個を仕分けし「今できる」だけ断定 | T9 と T15 `index.html` の `#providers` を実装に合わせて3群に作り直す / T16 `#next` の中身を実装に合わせて直す |
| R8 | 見出しの句読点 0件 | T14 `scripts/check-page.mjs` の句読点検査 |
| R9 | 絵文字 0件 | T14 `scripts/check-page.mjs` の絵文字検査 |
| R10 | 新しい色・角丸・グラデが0個 | T11 (`:root` の値だけを使う) |
| R11 | 「今できる」の各機能に UI と main の file:line がある | T17 `app-messenger` を読み取り専用で調べ、最終報告に仕分け表を出す |

## カテゴリ標準機能

このカテゴリ = **Mac アプリの紹介ページ (製品 LP)**。競合の実物 4つ
(Linear / Notion Calendar / Superhuman / Raycast) を 2026-08-03 に開いて、
当然ある要素を数えた。1要素 = 1行で扱いを宣言する。

| 当然ある要素 | 4つのうち何個が持っているか | 今回どうするか |
|---|---|---|
| 上に固定のヘッダー + ダウンロード/申込のボタン | 4/4 | 既にある (`.lg-header`)。触らない |
| 一番上の言い切り + 製品の画面の絵 | 4/4 | 既にある (ヒーロー)。文を1文に減らす |
| 機能の一覧 (短い見出し + 1〜2文) | 4/4 | **今回実装** (`#can`。23個) |
| 使う人の型ごとの入口 | 2/4 (Superhuman / Linear) | **今回実装** (`#audience`。3つ) |
| つながる先の一覧 | 3/4 | 既にある (`#providers`)。実装に合わせて3群に作り直す |
| これから足すもの / 変更履歴 | 3/4 | 既にある (`#next`)。実装に合わせて中身を直す |
| 値段の表 | 4/4 | **対象外**。コードに価格が1つも無く、購入の口も閉じている (`licensePublicKey.ts:61` `PURCHASE_READY = false`)。無い値段は書かない |
| お客様の声 / 導入企業のロゴ | 4/4 | **対象外**。実績の数字を捏造しない (竹蔵の指示)。実際の声が集まってから |
| よくある質問 | 3/4 | **後で**。今は困りごとが1つ (Messenger の履歴) しか分かっておらず、それは畳んだ注記に入れた |
| 会社概要 / 連絡先 / 規約 | 4/4 | 既にある (`#company` / footer) |
| 機械向けの情報 (llms.txt など) | 1/4 (どこも本格的にはやっていない) | **今回実装** (`#for-ai` + `llms.txt`)。ここが竹蔵の狙いで、競合が薄い所 |

## 5. 完了の判定 (測定できる後条件)

1. `node scripts/check-page.mjs` が「すべて通りました」で終わる (exit 0)
2. 1440x900 の browser で console のエラーが 0 件
3. 1440 と 390 の両方でスクリーンショットを撮り、横にはみ出す要素が 0 件
4. ページ全体の高さが 20.2画面から減っている
5. `app-messenger` に `git status` の変化が 1 件も無い (読み取り専用を守れたことの証拠)
