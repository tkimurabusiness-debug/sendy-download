# SPEC: sendy-lp-20260807-v0268 — 配布ページを 0.2.68 にそろえる

対象リポジトリ `/Users/kimuratakezou/uicos/sendy-download-page`
配布物の実体 `/Users/kimuratakezou/uicos/app-messenger/release/Sendy-0.2.68-*` (**読み取り専用**)

---

## 1. 要件一覧 (受入条件)

| # | 要件 | 受入条件 (機械または目視で確かめられる形) |
|---|---|---|
| R1 | ページに書いてある版を 0.2.68 にする | `rg -c '0\.2\.51' index.html llms.txt` が 0 件。`rg -c '0\.2\.68'` が index.html 5件 / llms.txt 2件 (版の表示2か所 + 新しい節の見出し3か所) |
| R2 | 0.2.68 で変わった6件を人が読める形で載せる | `#whats-new` の `.feat-item` が 6 件 |
| R3 | 同じ6件を AI 向けの節にも載せる | `#for-ai` の `<dt>` が 44 → 50 件。`node scripts/check-page.mjs` が llms.txt との一致を確認 |
| R4 | 見出し・ボタンの文言に「、」「。」を入れない | `node scripts/check-page.mjs` が 0 件 |
| R5 | 絵文字を1つも使わない | 同スクリプトが index.html と llms.txt で 0 件 |
| R6 | 0から CSS を書かない | `git diff --stat css/` が 0 行。既にある `.feat` `.feat-grid` `.feat-item` だけを使う |
| R7 | 実ブラウザで壊れていないことを確かめる | 1440 と 390 でスクリーンショット。console のエラー 0 件。横にはみ出す要素 0 件 |

## 2. 調査 (実測で裏取り)

- `/Users/kimuratakezou/uicos/app-messenger/package.json` の version = **0.2.68**
- `/Users/kimuratakezou/uicos/app-messenger/release/` に Mac 用4本と Windows 用2本が実在する
  (`Sendy-0.2.68-arm64.dmg` `Sendy-0.2.68.dmg` `Sendy-0.2.68-arm64-mac.zip` `Sendy-0.2.68-mac.zip`
  `Sendy-Setup-0.2.68.exe` `Sendy-0.2.68-win.zip`)
- `js/download.js` は落とすボタンの行き先を GitHub の「最新のリリース」から毎回引いている。
  ページに版を焼き込んでいない。よって**行き先を 0.2.68 にするにはリリースの公開が要る**
- GitHub の最新リリース (2026-08-07 01:05 に確認) = **v0.2.68** (公開時刻 2026-08-06T16:00:33Z)。
  Mac 用と Windows 用の6本が全部上がっている。
  **この作業の途中で竹蔵か別の作業者が公開した** (作業開始時点では v0.2.66 だった)。
  この作業では GitHub へ1バイトも上げていない

## 3. 実装プラン (どのファイルに何を書くか)

| ファイル | 何をするか |
|---|---|
| `index.html` 318行 | `今のバージョン (0.2.51)` → `(0.2.68)` |
| `index.html` 608行 | AI 向けの節の `<dd>0.2.51 (ベータ)</dd>` → `0.2.68` |
| `index.html` `#can` の直後 | `#whats-new` を新設。既にある `.feat` `.feat-grid` `.feat-item` だけで組む |
| `index.html` ヘッダーの案内 | `#whats-new` への行き先を1つ足す |
| `index.html` `#for-ai` | `<h3>この版 (0.2.68) で新しくなったところ</h3>` と `<dl>` 6件を足す |
| `llms.txt` 13行 | `- 版: 0.2.68 (ベータ)` |
| `llms.txt` | `## この版 (0.2.68) で新しくなったところ` を足す (`#for-ai` と同じ6件) |

## フォーク調査結果

**0から書くものが1つも無い作業**なので、外部から持ち込む物はない。

| 調べたもの | 採用したか | 理由 |
|---|---|---|
| 既にあるこのページの `.feat` `.feat-grid` `.feat-item` (`css/40-features.css` 7-47行) | **採用** | 「これから足すもの」の一覧が同じ形で動いている。新しい色も角丸も作らずに済む |
| 変更履歴を出す外部の部品 (keep-a-changelog の HTML 版など) | **非採用** | 依存パッケージを足さない約束 (`sendy-lp-20260803` の SPEC 10番)。6件を並べるだけなので既存の一覧で足りる |
| `js/download.js` を書き換えて版を焼き込む案 | **非採用** | 焼き込むと版が上がるたびに 404 になる (2026-07-25 に実際に起きたとコメントに残っている) |

## カテゴリ標準機能

このカテゴリ = **アプリ紹介ページの「新しくなったところ」の見せ方**。
実在する4つ (Linear / Notion / Raycast / Superhuman) の changelog を 2026-08-07 に見て、当然ある要素を数えた。

| 当然ある要素 | 4つのうち何個が持っているか | 今回どうするか |
|---|---|---|
| 版の番号を出す | 4/4 | **今回実装** (`#whats-new` の見出しと AI 向けの節) |
| 1件 = 短い名前 + 1〜2文の説明 | 4/4 | **今回実装** (`.feat-item` の `<b>` と `<span>`) |
| 直した物と足した物を並べる | 4/4 | **今回実装** (6件) |
| 過去の版もさかのぼって読める | 4/4 | **後で**。今は最新の1版だけ。増えたら別ページに分ける |
| 日付を出す | 3/4 | **対象外**。日付を出すと「更新が止まっている」と読まれる。版の番号で足りる |
| 画像や動画を付ける | 2/4 | **対象外**。撮り直しの手間が毎回増える。文字だけで伝わる6件 |
| RSS などの購読口 | 2/4 | **対象外**。既に「できたら知らせてほしい」の受け口がページにある |

## MECE宣言

- **軸 = 「どこに版が書いてあるか × 誰が読むか」**。
  - どこ = index.html の本文 / index.html の AI 向けの節 / llms.txt / 落とすボタンの行き先
  - 誰が = 人 / AI
- **漏れの点検**: `rg -n '0\.2\.[0-9]+'` をリポジトリ全体にかけ、当たった 5 行を1つずつ見た。
  うち `js/download.js` 6行目の `v0.2.45` `v0.2.49` は「Intel の Mac 版と Windows 版がいつから
  配られているか」の記録で、今の版ではない。**直さない**。
- **重複の点検**: 人が読む節と AI 向けの節は同じ事実を2回書くが、これは
  `sendy-lp-20260803` の R6 (人と AI を分ける) で決めた設計。`scripts/check-page.mjs` が
  ずれを機械で止めるので、重複しても食い違わない。
- **別の分け方なら結論が変わるか**: 「ファイル別」で分けると落とすボタンの行き先を見落とす。
  行き先はファイルの中に無く GitHub のリリースにあるため。今の軸を採る。

## 5. 完了の判定 (測定できる後条件)

1. `rg -c '0\.2\.51' index.html llms.txt` が 0 件
2. `node scripts/check-page.mjs` が「すべて通りました」で終わる (exit 0)
3. 1440x900 の実ブラウザで console のエラーが 0 件
4. 1440 と 390 の両方でスクリーンショットを撮り、横にはみ出す要素が 0 件
5. `git diff --stat css/` が 0 行 (CSS を1行も足していないことの証拠)
6. `/Users/kimuratakezou/uicos/app-messenger` に `git status` の変化が 1 件も無い

## 6. 竹蔵の判断が要る点

落とすボタンの行き先は**実ブラウザで 0.2.68 を指していることを確かめた**。
GitHub のリリース v0.2.68 が公開済みだったため (公開したのはこの作業ではない)。

残っている判断は1つだけ。**このページの変更を公開するかどうか**。
`git push` はしていない。押すと GitHub Pages が入れ替わり、来た人の目に触れる。

なお `/Users/kimuratakezou/uicos/app-messenger/openspec/changes/sendy-20260806-connection-rebuild/SPEC.md`
の45行目には「竹蔵1人にだけ配る。ベータ10人へは広げない。」と書いてある。
一方で GitHub のリリース v0.2.68 は誰でも落とせる形で公開されている。
**この2つは食い違っている。** どちらが今の方針かを竹蔵に確かめてから push する。
